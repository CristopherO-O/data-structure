os arquivos da pasta são:
    Graph.h:
        A Estrutura do grafo
    Graph.c:
        A implementação das funções do TAD
    Graph.txt:
        o arquivo que contem o grafo a ser lido
    Dijkstra.c:
        Main

---------------------------------------------------------------------------------
para compilar usa-se(compilei no windows):
    gcc Dijkstra.c Graph.c -o programa
    
